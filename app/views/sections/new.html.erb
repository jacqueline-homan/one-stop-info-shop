<%= link_to("<<Back to Sections List", {:action => 'index'}, :class => 'back-link') %>

<div class="sections new">
  <h2>Create Section</h2>

  <%= form_for(:section, :url => {:action => 'create'}) do |f| %>

    
    <div class="form-buttons">
      <%= submit_tag("Create Section") %>
    </div>
  <% end %>
</div> 
